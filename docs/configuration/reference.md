# Configuration Reference

Configuration loads from explicit runtime overrides, environment variables, `.env`, and safe defaults in that order.

No real secrets may be committed. `.env.example` documents variable names only.

