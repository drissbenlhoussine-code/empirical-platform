# Local Infrastructure Guide

Local dependency configuration lives in `infra/local/compose.yaml`.

It defines development-only PostgreSQL and S3-compatible object storage services. It does not create business schemas, buckets containing evidence, production credentials, or empirical campaign data.

