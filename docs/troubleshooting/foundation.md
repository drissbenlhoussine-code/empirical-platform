# Troubleshooting

## Python Version

The approved runtime is Python `>=3.13,<3.14`. If commands fail under Python 3.14 or 3.12, install or select Python 3.13.

## Tooling

Run `python -m pip install -e ".[dev,test,security,build]"` before quality commands.

