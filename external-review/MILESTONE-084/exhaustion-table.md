# MILESTONE-084 — Exhaustion Table

**27 required items. 27 EXECUTED_PASS, 0 EXECUTED_FAIL_BLOCKER.**

Generated by `tools/render_m084_exhaustion_table.py`; do not edit by hand.

Two statuses exist and no others. `EXECUTED_PASS` means the item was run
and passed; `EXECUTED_FAIL_BLOCKER` means it was run and failed.
"Documented", "reviewed", "by construction", "not needed", "previously
passed" and "covered continuously" are not substitutes for execution, and
this table cannot express them.

Each status is DERIVED, not written: the renderer re-reads the artifact the
item produced or re-runs the gate, and a row whose evidence is absent or
does not say what the row claims renders as a blocker. A table that could be
edited to read EXECUTED_PASS would be a list of intentions.

| Section | Required item | Status | Evidence |
|---|---|---|---|
| §3 | Database concurrency campaign, executed with real resets | **EXECUTED_PASS** | 36 races, three repetitions, distinct oids (`concurrency-results.md`) |
| §4 | All 27 mutation families mutated, detected, restored, re-verified | **EXECUTED_PASS** | 27 of 27 detected (`mutation-matrix.md`) |
| §4 | No mutation family survives at head | **EXECUTED_PASS** | no SURVIVED row |
| §5 | Hostile pass 1 — scientific authority (>=25) | **EXECUTED_PASS** | 27 attacks (minimum 25), 0 findings |
| §5 | Hostile pass 2 — database adversary (>=40) | **EXECUTED_PASS** | 51 attacks (minimum 40), 0 findings |
| §5 | Hostile pass 3 — trading-risk adversary (>=35) | **EXECUTED_PASS** | 39 attacks (minimum 35), 0 findings |
| §5 | Hostile pass 4 — operator and product adversary (>=30) | **EXECUTED_PASS** | 34 attacks (minimum 30), 0 findings |
| §5 | Hostile pass 5 — software-governance adversary (>=30) | **EXECUTED_PASS** | 32 attacks (minimum 30), 0 findings |
| §6 | Performance characterized at 0/1/10/100/1k/10k/25k with median, p95, max, samples | **EXECUTED_PASS** | every scale measured with sample counts (`performance-results.md`) |
| §6 | Query plans captured per scale | **EXECUTED_PASS** | EXPLAIN (ANALYZE, BUFFERS) per scale (`performance-results.md`) |
| §6 | Row-lock duration measured under real contention | **EXECUTED_PASS** | waiter blocked on transactionid (`performance-results.md`) |
| §7 | Broker research separates verified from unverified sources | **EXECUTED_PASS** | three explicit verification tiers (`broker-and-market-data-research.md`) |
| §7 | Blocked URLs recorded with an operator-verification checklist | **EXECUTED_PASS** | per-URL questions, none marked complete (`operator-verification-checklist.md`) |
| §7 | Conclusion classified CONDITIONAL where evidence is secondary | **EXECUTED_PASS** | conclusion is conditional (`broker-and-market-data-research.md`) |
| §8 | Regression: PostgreSQL ON, base and candidate, equally fresh databases | **EXECUTED_PASS** | base vs candidate, sets diffed (`validation-results.md`) |
| §8 | Regression: PostgreSQL OFF, base and candidate | **EXECUTED_PASS** | base vs candidate, sets diffed (`validation-results.md`) |
| §8 | Regression: clean database through the full migration history | **EXECUTED_PASS** | 20 up, 20 down to base, 20 up again (`validation-results.md`) |
| §8 | Regression: clean installed wheel | **EXECUTED_PASS** | walkthrough runs from site-packages (`validation-results.md`) |
| §9 | Operator walkthrough executed against an installed wheel | **EXECUTED_PASS** | 18 steps, all at their expected exit code (`validation-results.md`) |
| §9 | At least three NO_TRADE demonstrations with distinct reasons | **EXECUTED_PASS** | three distinct reasons (`validation-results.md`) |
| §10 | Frozen M083 files unmodified since the base commit | **EXECUTED_PASS** | frozen-path guard — exit 0 |
| §10 | Frozen M083 acceptance obtained at M083's own revision | **EXECUTED_PASS** | 51 passed at the frozen commit (`validation-results.md`) |
| §10 | Architecture order-submission deny-list holds | **EXECUTED_PASS** | architecture gate — exit 0 |
| §10 | Authority document is the deterministic rendering of its contract | **EXECUTED_PASS** | authority renderer --check — exit 0 |
| §10 | File-audit matrix is the rendering of the real diff | **EXECUTED_PASS** | file-audit --check — exit 0 |
| §10 | Mechanical suppression accounting published | **EXECUTED_PASS** | counted, not estimated (`validation-results.md`) |
| §10 | No coverage pragma or skipped test in the M084 diff | **EXECUTED_PASS** | noqa 98, type-ignore 117, coverage pragmas 0, skipped tests 0 |
